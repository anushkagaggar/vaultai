class State:

    PENDING = "pending"
    LOCKED = "locked"
    RUNNING = "running"

    SUCCESS = "success"
    FALLBACK = "fallback"
    SUPPRESSED = "suppressed"

    FAILED = "failed"
    CANCELLED = "cancelled"
